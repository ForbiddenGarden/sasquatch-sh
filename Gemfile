source "https://rubygems.org"

gem "jekyll", "~> 4.3"

group :jekyll_plugins do
  gem "jekyll-feed", "~> 0.17"
end

# Vercel builds on Linux. If you regenerate Gemfile.lock on another platform,
# re-run: bundle lock --add-platform x86_64-linux
